import PhotoCarousel from '@/components/gallery/photoCarousel/photoCarousel';

export default function ProductsPage() {
  return (
    <main>
      <PhotoCarousel category="products" />
    </main>
  );
}